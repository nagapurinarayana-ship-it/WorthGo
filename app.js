const state={city:'Hyderabad',mode:'local'};const $=s=>document.querySelector(s);const $$=s=>document.querySelectorAll(s);let toastTimer;function toast(msg){let el=$('#toast');if(!el){el=document.createElement('div');el.id='toast';el.className='toast';document.body.appendChild(el)}el.textContent=msg;el.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>el.classList.remove('show'),2200)}function updateContext(){const mode=state.mode==='local'?'For locals':state.mode==='visitor'?'For visitors':'For curious explorers';$('#contextLabel').textContent=`${mode} in ${state.city}`}function setMode(mode){state.mode=mode;$$('.mode').forEach(b=>{const active=b.dataset.mode===mode;b.classList.toggle('active',active);b.setAttribute('aria-selected',String(active))});updateContext();toast(mode==='local'?'Local mode selected':mode==='visitor'?'Visitor mode selected':'Explore mode selected')}$$('.mode').forEach(b=>b.addEventListener('click',()=>setMode(b.dataset.mode)));$$('[data-mode-jump]').forEach(b=>b.addEventListener('click',()=>{setMode(b.dataset.modeJump);$('.finder').scrollIntoView({behavior:'smooth',block:'center'})}));$$('.intent').forEach(b=>b.addEventListener('click',()=>{const name=b.querySelector('strong').textContent;$$('.intent').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');toast(`${name} selected — your WorthGo picks will start here.`)}));$$('[data-filter]').forEach(b=>b.addEventListener('click',()=>toast(`${b.textContent.trim()} — filters are ready for the next discovery step.`)));$$('[data-action="finder"]').forEach(b=>b.addEventListener('click',()=>$('.finder').scrollIntoView({behavior:'smooth',block:'center'})));$('.city-pill').addEventListener('click',()=>{state.city=state.city==='Hyderabad'?'Dubai':'Hyderabad';$('.city-pill').innerHTML=(state.city==='Hyderabad'?'🇮🇳 Hyderabad':'🇦🇪 Dubai')+' <span>⌄</span>';updateContext();toast(`${state.city} selected`)});$('.menu-btn').addEventListener('click',()=>toast('WorthGo navigation will grow with the site — homepage first.'));$$('.save').forEach(b=>b.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();b.textContent=b.textContent==='♡'?'♥':'♡';toast(b.textContent==='♥'?'Saved locally':'Removed from saved places')}));updateContext();